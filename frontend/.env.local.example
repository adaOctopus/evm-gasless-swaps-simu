NEXT_PUBLIC_API_URL=http://localhost:4000
# Alchemy API key (dashboard.alchemy.com) for smart account + bundler RPC
NEXT_PUBLIC_ALCHEMY_API_KEY=
# EIP-7702: set to "true" to use Type 4 gasless flow (delegator contract); requires NEXT_PUBLIC_DELEGATOR_CONTRACT_ADDRESS
# NEXT_PUBLIC_USE_EIP7702=
# EIP-7702: delegator contract address (optional; else use constants per chain)
# NEXT_PUBLIC_DELEGATOR_CONTRACT_ADDRESS=
